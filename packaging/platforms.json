{
  "schema": "opensocrates.packaging-platform-candidates/1.0.0",
  "decision_gate": "DG-01",
  "candidate_platforms": [
    "darwin-arm64",
    "darwin-x64",
    "linux-x64",
    "windows-x64"
  ],
  "observed_local_platforms": [
    "darwin-arm64"
  ],
  "release_targets": [
    "darwin-arm64"
  ],
  "release_claim_status": "darwin-arm64_only",
  "shipped_launchers": {
    "darwin-arm64": "bin/launch.sh"
  },
  "unvalidated_platforms": [
    "darwin-x64",
    "linux-x64",
    "windows-x64"
  ],
  "startup_budget": {
    "darwin-arm64": {
      "status": "release_gate_required",
      "measurement": "packaged_codex_session_start",
      "minimum_samples": 20,
      "configured_timeout_ms": 2000,
      "required_p95_max_ms": 1000,
      "evidence": "build/evidence/codex-session-start-timing.json",
      "synthetic_probe_evidence": "build/evidence/dg-01-macos.json"
    }
  },
  "signing_status": "unvalidated",
  "notes": "Only darwin-arm64 is a release target and only bin/launch.sh is shipped. The synthetic DG-01 probe is packaging evidence, not a real-hook latency claim; release requires the generated Codex SessionStart gate. Other entries are dry-run build candidates, are not shipped, and carry no support claim."
}
