# OpenSocrates internal method catalog

Content revision: `{{CONTENT_REVISION}}`

This catalog is internal to the `opensocrates` skill. It is an index, not a
procedure and not grounding evidence. Select the smallest relevant set, then
read each linked procedure completely before applying, naming, or citing it.

{{METHOD_REFERENCE_INDEX}}
