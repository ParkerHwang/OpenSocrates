# Status

This is the explicitly supported short name for the content-free status
projection. It invokes the compatibility dispatcher with the exact name
`status` and injected `project_status` action; no other command aliases are
defined.

`${PLUGIN_ROOT}/bin/launch.sh control codex`

`opensocrates.application.compatibility.dispatch_compatibility("status", services=...)`
