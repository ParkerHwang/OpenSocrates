# OpenSocrates status

Show content-free installation, capability, trust, and public-record status
through the injected `project_status` action. The exact compatibility command
name is `opensocrates-status`.

Fixed control boundary:

`${PLUGIN_ROOT}/bin/launch.sh control codex`

The source seam is:

`opensocrates.application.compatibility.dispatch_compatibility("opensocrates-status", services=...)`
