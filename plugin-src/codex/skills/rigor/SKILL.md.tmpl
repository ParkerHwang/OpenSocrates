---
name: rigor
description: Apply the declared raise-only rigor level to judgment work.
---

# Rigor

{{FRAGMENT_PARTICIPATION_RIGOR_EN}}

{{FRAGMENT_STRICT_SECOND_PASS_EN}}

한국어 안내:

{{FRAGMENT_PARTICIPATION_RIGOR_KO}}

{{FRAGMENT_STRICT_SECOND_PASS_KO}}

The fixed package controller boundary below is documentation for the host
integration, not a shell instruction for the model:

`${PLUGIN_ROOT}/bin/launch.sh control codex`

Never execute that control command directly, substitute a filesystem path, or
fabricate its typed stdin payload. Only the host integration may invoke it.
