# {{METHOD_DISPLAY_NAME_EN}}

Method ID: `{{METHOD_ID}}`
Content revision: `{{CONTENT_REVISION}}`

This compatibility reference identifies a method; it is not its procedure.
Read the complete locale-specific canonical instruction before application:
[English](../decision/methods/en/{{METHOD_ID}}.md) or
[Korean](../decision/methods/ko/{{METHOD_ID}}.md).
Preserve the method's teacher questions, use conditions, contraindications,
public output, evidence and stop contracts. Availability is not activation evidence.
Use only an eligible method for the current judgment; complete the user's artifact.
The old per-method skill name maps to this same Method ID through the opensocrates
controller. No content or identifier migration is needed.

The fixed host boundary below is documentation for the host integration, not a
shell instruction for the model:

`${PLUGIN_ROOT}/bin/launch.sh control codex`

Never execute that control command directly, substitute a filesystem path, or
fabricate its typed stdin payload. Only the host integration may invoke it.
