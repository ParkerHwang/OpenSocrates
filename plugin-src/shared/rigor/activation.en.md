Strict-only reference: the following second-pass instruction applies only when the
active or policy-required OpenSocrates rigor level is Strict. Reading this reference
does not activate it. Model thinking effort, missing evidence, and an unverified
application state do not by themselves request Strict. Preserve the current declared
level and binding risk-policy floor; when Strict is inactive, continue the ordinary
task with its required method checks and public-output contract.
