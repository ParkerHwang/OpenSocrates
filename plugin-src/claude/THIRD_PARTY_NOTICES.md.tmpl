# Third-party notices

The generated Claude package includes a host-specific OpenSocrates bundled
onedir runtime. The Claude selector invokes the user's existing Claude Code CLI
and adds no Anthropic SDK dependency. This runtime excludes the OpenAI Codex SDK
and CLI packages; it includes Pydantic and its transitive/native dependency
closure in addition to Python standard-library code.

For the exact dependency inventory and terms, consult the accompanying release
SBOM and the license files bundled with the runtime where provided.

Bundle source identity: `{{SOURCE_TREE_HASH}}`.
