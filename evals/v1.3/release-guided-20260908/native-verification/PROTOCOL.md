# Single affected native-first behavior verification

Prospective source32d0322c73c8c116b961ddb3a68a75473c715466; freeze exact package
and driver identities after native release validation and normal isolated update.
P09/P10 future-condition failures and P10 blind-pass/adversarial-revision remain.
The distinct repair prioritizes the existing native eligibility path on usable
Claude/Codex, preserves known absence features and genuine failure fallback,
keeps empty eligible selection valid, and gives a concrete per-question public
output pattern. No new semantic validator, backend or authored-procedure rewrite.

ONE KO forecast invocation, same unmodified fixture/task/model gpt-5.6-luna/max,
default-deny environment, apps/multi_agent disabled and no network. No private
commands or diagnosis are supplied to the actor. The supported package itself
contains the new routing guide. Primary300 result remains independent of the
prospective600 finite ceiling. No repeat-until-pass. Preserve all earlier38 calls.

Check actual supported native invocation separately from artifact fidelity.
Capture only declared closed feature codes from complete JSON literals in an
observed native invocation when its context matches the returned selection;
no raw command, context IDs, tool output or private reasoning is persisted. If
no such literal is observable, report that gap rather than infer the features.
Selection output alone cannot verify semantic feature classification/application.
Controlled credential-free packaged CLI cases separately verify exclusions and
ordinary eligible routing. Neither layer substitutes for the other.

Require correct current and future claims, scoped reopening conditions, final
turn/artifact, authority preservation and eligible complete canonical delivery.
Send a new blinded P11 artifact to the independent KO reviewer, with no diagnosis
or previous judgments. Scope any success to this one observed case, never a
universal guarantee or performance/quality improvement claim. No Claude calls.
