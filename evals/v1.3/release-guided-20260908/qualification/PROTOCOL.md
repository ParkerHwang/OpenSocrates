# Strict applicability repair: affected-case qualification

Freeze before outcomes. Source95a96a1dfebd62ab225c4424f444ee34cebbbd85 and the
normal installed package hash are bound in freeze.json. The control-reference
repair conditions the existing Strict second-pass text on an active/policy-required
Strict level, and narrows optional rigor/trace discovery. It preserves explicit
Strict, policy floors, the original fragment, required public/card outputs and all
96 authored procedures. No presentation profile, timeout or semantic task criterion
changes. Nine policy/activation regressions pass, including default vs explicit
and policy-required Strict paths.

The unchanged4ea diagnostic actually delivered complete rigor/trace/controller
references and the unconditional Strict fragment. At300seconds the CLI was alive,
turn.completed was absent, and no recorded shell command or native stream was
active. This excludes a completed turn merely waiting for a stream in that run;
it does not expose model reasoning or establish a sole cause.

Run ONLY the three still-affected J cases: changedKO, forecastEN, forecastKO.
Same follow-up task/helper and critical criteria; gpt-5.6-luna/max;300seconds;
concurrency2; one attempt each. Keep all prior24+8+1 diagnostic outcomes. No
relabeling or timeout inflation. Default-deny environment and delayed audit facts
remain. Explicitly disable multi_agent as well as apps, enforcing the previously
stated no-delegation condition. Record this setting change, not a causal isolation
claim. No private product command is pre-taught.

Record content-free arrival timestamps for public/tool events, known complete
reference deliveries, active tool categories, turn.completed presence and process
state. Distinguish final delivery from process/stream closure. Do not retain raw
commands, tool output, reasoning or identifiers. Unknown gaps are not private
thinking observations. Preserve whole public artifact metrics and sanitized output.
Every case must deliver its requested final response and artifact within the same
unchanged bound, retain facts/authority and eligible canonical guidance. Missing
or wrong results remain failures requiring disposition before merge.
