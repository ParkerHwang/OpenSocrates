# Focused completion and evidence-order follow-up

Frozen before outcomes. This follows the original24attempts without removing or
relabeling them: U/G completed16/16; installedJ completed4/8, with4 final-delivery
timeouts at300s despite all requested files existing. Shared conditional fee wording
is an ambiguous input finding, not a proven JIT regression. Source access to the
old reveal helper could expose future facts, so strong information-order claims
were not established. Inherited Desktop control/IPC variables were another harness
limitation; their causal effect on timeouts is unknown.

Current source4ea8542491f7d195734e42d597e328acf929bfcc clarifies the existing general
completion contract: unverified application is an instrumentation limit, not an
unfinished task or demand for unavailable proof. Required method checks/stop
conditions remain binding. Authored procedures and presentation profiles are
unchanged. This is not a semantic rewrite or a length/iteration cap.

Use the exact rebuilt/installed package hash recorded by freeze.json. Four affected
behaviors (changed evidence and unsupported forecasting, each EN/KO) run in two
arms: upfrontGuided G and normally installedJITGuided J. Eightcalls total,
gpt-5.6-luna/max, CLI0.145.0, same300second deadline, concurrency2, one attempt.
No new private command or method is pre-taught to J. Keep the original critical
criteria; additionally make premium support explicitly optional/declinable in the
new audit fact, so an optional fee cannot become a current required prerequisite.

The driver now starts from a default-deny environment: PATH, locale/shell and
explicit owned HOME/CODEX_HOME/TMPDIR/state paths only. No Desktop IPC/config,
permission override, API key or unrelated parent environment is inherited. Normal
browser-login credentials remain in the owned profiles; they are not copied.
Each CLI owns a separate process group, terminated at deadline and cleaned afterward.

The reveal helper contains no future facts. The harness waits for a syntactically
valid initial public artifact with top-level choice, records its hash/text, and
only then atomically materializes the new audit file. The helper checks actual
shell HOME/CODEX_HOME/temp boundaries and waits at most5seconds for that file.
Record initial observation, audit materialization and command/artifact order.
Private reasoning order remains unobservable. An unrelated file read is not itself
a failure; future facts must not exist in the agent workspace before the gate.

The revised environment, helper and controller clarification change together;
this qualification cannot isolate which caused any completion difference. It is
a focused current-source qualification, not a replacement superiority study.
Keep all failure/timeout denominators and public-only/privacy-filtered evidence.
Assess actual requested files, final delivery, authority, source certainty and
protected semantics separately. No timeout increase or post-outcome criteria change.
