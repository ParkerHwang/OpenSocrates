# One prospective finite-completion observation

Frozen before execution. Exactly one new-source Korean forecast invocation using
8109176a353842e7d6a2612a4eda8db41988241c and the package hash in freeze.json.
The Bayesian catalog now preserves its already-authored prior/direction
prerequisites. All 96 procedure bodies are unchanged. This repairs metadata
coverage; prior KO artifacts did not establish invalid Bayesian application.

The original 300-second study result remains independently recorded. If still
running at 300 seconds, persist a primary deadline marker immediately; observe
only until 600 seconds total for a separate finite-completion result. Never
relabel earlier failures, the 300-second failure, or rerun until success. This
prospective observation is release-owner-authorized because the lab budget is
not a published host latency SLO. A late correct completed turn can support a
finite-completion claim only, with explicit Luna/max latency limitations.

Same synthetic KO task, default-deny environment, normal installed package,
Luna/max, apps and multi_agent disabled, network disabled, isolated owned HOME,
CODEX_HOME and temporary roots. No private commands pre-taught. Required: final
turn, correct artifact, authority/factual fidelity, and eligible canonical
retrieval; application stays unverified. Review the new artifact independently
as a distinct packet. Preserve all initial/follow-up/diagnostic/qualification
records and their input/configuration differences. No quality superiority claim.
