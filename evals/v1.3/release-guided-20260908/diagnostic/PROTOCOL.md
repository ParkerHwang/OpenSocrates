# One-call completion mechanism diagnostic

One instrumented EN forecasting call on the unchanged installed4ea8542 candidate,
with the follow-up's same task, model gpt-5.6-luna/max, default-deny environment and
300second limit. This is not a retry counted as qualification and cannot erase the
prior failures. Freeze before the call. Record event times/types, complete delivery
of known rigor/trace/method references, active tool modes, public second-pass markers,
turn.completed presence and whether the CLI process is alive at deadline. Do not
read or save reasoning content, raw tool responses, private identifiers or commands.
No active tool during a gap does not prove what the model was thinking.

This distinguishes a missing final turn from a completed turn whose process/stream
stays alive, and checks long-lived decision streams vs unrequested control guidance.
The separately prepared Strict-reference applicability guard is not installed in
this diagnostic. No timeout increase, hidden thought inspection or broad study.
