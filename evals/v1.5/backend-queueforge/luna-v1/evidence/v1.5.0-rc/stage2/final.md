Implemented the follow-up in the existing queue paths. New and migrated stores use schema version 2; submissions retain idempotency and enforce pending capacity in the write transaction, while claims reconcile expired leases, apply backoff, and enforce inflight capacity. Claims use priority then creation order. Added cancellation and tenant/filter-bound snapshot listing.

Migration preserves the version 1 table data and idempotency records, assigns historical jobs retry base zero, and checks for unsupported future versions before changing journal settings. The claim index supports priority/FIFO ordering; pending and inflight indexes support the transactional tenant-scoped capacity counts. These choices are documented in [README.md](<WORKSPACE>/README.md:14).

**Verification:** `go test ./...`, `go test -race ./...`, `go build`, and `git diff --check` passed. Loopback checks covered concurrent cross-process quota enforcement, replay, inflight limits, restart durability, and shared-database startup. I migrated the existing workspace v1 database through two server processes: its two ready jobs and both idempotency replays were preserved. That database had no leased or completed jobs, so separate migration tests covered lease continuity and preservation of completion results. `go.mod` and `go.sum` are unchanged. The Python loopback harness emitted a macOS cache-file warning, but its assertions completed successfully. Owned server processes were stopped.

Changed files: [queue.go](<WORKSPACE>/internal/queue/queue.go:130), [http.go](<WORKSPACE>/internal/queue/http.go:62), [main.go](<WORKSPACE>/cmd/server/main.go:21), and [queue_test.go](<WORKSPACE>/internal/queue/queue_test.go:183). The task checkpoint was refreshed and inspected. No commit was made.
