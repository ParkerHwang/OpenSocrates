You own only this disposable QueueForge workspace and its declared private memory
data. Implement the requested backend completely; resolve routine details from
the contract. Do not inspect parent/sibling projects, evaluator/checker code, other
conditions or previous outcome summaries. Do not delegate or call another model.
No external network/services, credentials, real projects, active plugin changes,
global settings, messages, publishing or deployment. Loopback tests are authorized;
dependencies are predownloaded and Go module networking is off.

Preserve the supplied driver helper/test and exact go.mod/go.sum. Use Go1.26.3 and
the standard library plus the pinned SQLite driver, with no new runtime dependency.
Do not commit; the coordinator records source commits. Keep owned test processes
under this workspace and stop them. Code and development documents are English.
Read BRIEF.md and current files; do not seek AGENTS outside this workspace.

Current source and the task contracts govern facts. Notes/memory retain bounded
public intent/progress, never raw prompts, transcripts, code dumps, tool dumps or
hidden reasoning. A failed continuity operation does not erase constraints or stop
independent implementation work. Report actual checks, failures and remaining limits
in at most800 words. No emitted pack or self-reported completion replaces verification.
