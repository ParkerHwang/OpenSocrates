Implemented the v2 backend in the existing submission and transaction paths. It now supports scheduled priority claims, retry backoff, tenant pending and queue inflight limits, cancellation, and snapshot keyset listing. [store.go](<WORKSPACE>/internal/queue/store.go:228) retains the shared single and batch submission path; [http.go](<WORKSPACE>/internal/queue/http.go:248) routes both through it.

Schema migration runs in an immediate transaction. Existing v1 jobs retain priority 0 and retry base 0, and their original idempotency keys still replay. The claim index supports priority and FIFO order, the expiry index finds leases to reconcile, and the pending index supports quota counts. [README.md](<WORKSPACE>/README.md) and [PROJECT_NOTES.md](<WORKSPACE>/PROJECT_NOTES.md) are updated.

**Verification passed:** `go test -race ./...`, build, `git diff --check`, the existing two-process restart check, and a live check that created v1 data with the previous executable before starting two v2 processes. That check covered replay and lease continuity, scheduling, quotas, cancellation, and pagination with an intervening insert. A future-version database was rejected with its file bytes unchanged. New focused tests cover expiry boundaries, concurrent admission, and batch rollback.

Two intermediate check assertions were corrected: one expected capacity before the configured limit, and one expected a retry before its deadline-based backoff. Final reruns passed. No commit was made. SQLite still serializes writes and can return 503 after prolonged contention; worker effects remain at least once.
