Fixed the migrated version 1 idempotency replay failure. [service.go](<WORKSPACE>/internal/queueforge/service.go:385) now rejects an explicitly added version 2 scheduling field under an original version 1 key, even when its value is the default. New version 2 keys still treat omitted and explicit defaults as equivalent. I added regression cases in [followup_test.go](<WORKSPACE>/internal/queueforge/followup_test.go:157) and updated the [README](<WORKSPACE>/README.md) and [project notes](<WORKSPACE>/PROJECT_NOTES.md).

Go 1.26.3 `go test -race ./...`, the required build, and `git diff --check` pass. The independent frozen API and load checks have **not** been rerun on this repair. Their earlier load result failed its correctness gate and does not establish a throughput gain, so I made no speculative storage optimization. I started no server process. OpenSocrates native activation was unconfirmed; no canonical method was applied.
