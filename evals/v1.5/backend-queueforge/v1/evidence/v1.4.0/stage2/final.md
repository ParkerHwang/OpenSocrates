Implemented the version 2 backend in [service.go](<WORKSPACE>/internal/queueforge/service.go:60) and added the admission flags in [main.go](<WORKSPACE>/cmd/server/main.go). Migration preserves version 1 jobs, leases, results, and idempotency; startup rejects future schema versions before changing journal mode.

Single and batch submissions still use the same transaction path ([submit](<WORKSPACE>/internal/queueforge/service.go:348)). Claims and lifecycle writes retain transactional fencing while adding priority, scheduled availability, retry backoff, quotas, and cancellation. Listing uses a creation-sequence cursor. The new indexes support claim order, inflight and pending counts, and queue-filtered listing; availability is checked separately so a future high-priority job cannot block an eligible job. The API and limits are documented in [README.md](<WORKSPACE>/README.md).

**Verification passed:** `go test ./...`, `go test -race ./...`, and `git diff --check` on Go 1.26.3. [Follow-up tests](<WORKSPACE>/internal/queueforge/followup_test.go) cover retry boundaries, priority and FIFO order, cancellation races, batch rollback, pagination during insertion, migration, and future-version byte preservation. A loopback check used the preceding executable to create real version 1 data, then verified replay, lease and completion continuity, and pending and inflight contention across two new processes. Its cleanup stopped the owned processes; direct process-table inspection was unavailable in this sandbox.

The supplied driver files and exact `go.mod`/`go.sum` remain unchanged. No commit was made. [PROJECT_NOTES.md](<WORKSPACE>/PROJECT_NOTES.md) records bounded progress. The service still does not execute payloads; attempts are at least once, with fenced completion.
