# Authorship, review, and independence

The nine task definitions and their hidden reference assertions were authored by
a separate Codex subagent requested as `gpt-6-sol` with `high` effort. It received
the specification, bounded strata, ownership, and acceptance criteria, without
the completed old outcomes or old task solutions. Its isolated commits were
`6b2b5f82c76828157f3dc2c33cdd9c1a3b9dca94` and
`bd80e4b7736025999ffb4459d397a7188a636ba9`, integrated as `feac237` and `c90a365`.
The integrator reviewed the manifests and hidden checks, requested pre-outcome
schema/seed/amount clarifications, and verified the positive/negative checker
fixtures. No hidden assertions or reference solution files were copied into an
outcome model's workspace.

Two separately requested `gpt-6-sol`/medium read-only lanes audited old evidence
and host/harness behavior. Their findings were checked against actual records and
synthetic preflight tests before changes were integrated. These are AI engineering
reviews, not independent human quality ratings. No subagent served as a stronger
model inside a Luna-only outcome cell. Per-cell `multi_agent` was disabled.

The integrator authored the H02 delivery fixture, the focused guide-3 diagnostics,
the harness/instrumentation repairs, and evidence synthesis. Those fixtures are
explicitly separate from the nine independently authored task-quality fixtures.
Their diagnosis responds to observed failures; it is not a held-out discovery.

Subagent tool requests identify requested model and effort, not an independently
echoed backend identity. Per-subagent authoring/audit token categories, wall time,
and billing were not exposed as a complete accounting receipt and are `null` for
cost-comparison purposes. They are preparation overhead outside the frozen
outcome arms, never assumed to be free. CLI treatment/probe usage is recorded
separately and does not include these authoring costs or the primary agent's work.

No repository-competent bilingual human assessor was assigned or available through
this task. No external recruitment or message was sent. Blinded packet preparation
does not count as review. Human scores and model-judge quality scores remain null.
The requested next resource is an authorized reviewer who can assess the coding
artifacts and English/Korean collaboration packets under the fixed procedure.
