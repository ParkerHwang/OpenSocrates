{
  "artifacts": {
    "invoice.py": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Line:\n    sku: str\n    unit_cents: int\n    quantity: int\n\ndef quote(lines, region, member=False):\n    if region not in {'domestic', 'international'}:\n        raise ValueError('region')\n    subtotal = 0\n    for line in lines:\n        if line.unit_cents < 0 or line.quantity < 1:\n            raise ValueError('line')\n        subtotal += line.unit_cents * line.quantity\n    discount = subtotal // 10 if member and subtotal >= 5000 else 0\n    merchandise = subtotal - discount\n    shipping = 0 if subtotal == 0 else (\n        0 if region == 'domestic' and merchandise >= 5000\n        else 700 if region == 'domestic'\n        else 2000\n    )\n    return {\n        'subtotal_cents': subtotal,\n        'discount_cents': discount,\n        'shipping_cents': shipping,\n        'total_cents': merchandise + shipping,\n    }\n"
  },
  "public_final": "Updated [invoice.py](<disposable>/workspace/invoice.py) to add the optional `member` argument, apply the qualifying discount, calculate shipping from the post-discount amount, and return all four integer fields.\n\nVerified with inline checks for legacy calls, discount and shipping thresholds, international and empty orders, and invalid inputs. `python -m py_compile invoice.py` also completed successfully."
}
