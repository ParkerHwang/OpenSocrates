{
  "artifacts": {
    "retry.py": "def should_retry(status, method='GET'):\n    if status == 429:\n        return True\n    return status == 503 and method.upper() in {'GET', 'HEAD', 'PUT', 'DELETE'}\n\ndef delay_seconds(attempt):\n    if attempt < 1:\n        raise ValueError('attempt')\n    return min(2 ** (attempt - 1), 16)\n",
    "action.json": "{\n  \"retry_429_all_methods\": true,\n  \"retry_503_methods\": [\"GET\", \"HEAD\", \"PUT\", \"DELETE\"],\n  \"correction_applied\": true,\n  \"question_answer\": \"Retry 429 for every method; retry 503 only for GET, HEAD, PUT, and DELETE, case-insensitively; retry no other status.\",\n  \"verification\": [\n    \"Retry matrix passed for six statuses and thirteen method spellings.\",\n    \"Default method, delay values, and invalid-attempt ValueError checks passed.\"\n  ]\n}\n"
  },
  "public_final": "Updated [retry.py](<disposable>/workspace/retry.py) and created [action.json](<disposable>/workspace/action.json).\n\nVerified the retry rules across six statuses and thirteen method spellings, the default method, delay values, invalid-attempt errors, and `action.json`’s exact fields and types."
}
