{
  "artifacts": {
    "offer.py": "from catalog import lookup\n\ndef quote(label, seats, savings_cents=0):\n    return breakdown(label, seats, savings_cents)['total_cents']\n\ndef breakdown(label, seats, savings_cents=0):\n    plan = lookup(label)\n    if seats < 1 or seats > plan['max_seats']:\n        raise ValueError('seats')\n    subtotal = plan['seat_cents'] * seats\n    if type(savings_cents) is not int or savings_cents < 0 or savings_cents > subtotal:\n        raise ValueError('savings_cents')\n    return {\n        'plan_code': plan['code'],\n        'subtotal_cents': subtotal,\n        'savings_cents': savings_cents,\n        'total_cents': subtotal - savings_cents,\n    }\n"
  },
  "public_final": "Updated [offer.py](<disposable>/workspace/offer.py) with `breakdown()`. It uses the current `catalog.lookup` for the plan code, price, alias, and seat limit, and returns the requested dictionary. `quote()` still returns an integer through the same calculation.\n\nVerified both functions for every current plan, including `squad`, at minimum and maximum seats; checked zero and full-subtotal savings, plus invalid savings and out-of-limit seats. All checks passed. The existing change to `catalog.py` was left untouched."
}
