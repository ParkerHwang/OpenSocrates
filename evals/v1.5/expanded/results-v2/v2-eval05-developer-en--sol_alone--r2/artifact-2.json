{
  "artifacts": {
    "retry.py": "def should_retry(status, method='GET'):\n    return status == 429 or (\n        status == 503 and method.upper() in {'GET', 'HEAD'}\n    )\n\ndef delay_seconds(attempt):\n    if attempt < 1:\n        raise ValueError('attempt')\n    return min(2 ** (attempt - 1), 16)\n",
    "action.json": "{\n  \"retry_429_all_methods\": true,\n  \"retry_503_methods\": [\"GET\", \"HEAD\"],\n  \"correction_applied\": true,\n  \"question_answer\": \"A request is idempotent when repeating it has the same intended effect on server state as sending it once.\",\n  \"verification\": [\n    \"Checked statuses 100 through 599 across eight methods and case variants.\",\n    \"Checked default method, delay values for attempts 1 through 7, and ValueError for attempts 0 and -1.\",\n    \"Checked action.json field names and types.\"\n  ]\n}\n"
  },
  "public_final": "Updated [retry.py](<disposable>/workspace/retry.py) and [action.json](<disposable>/workspace/action.json): 503 now retries only for GET and HEAD, case insensitively. 429 still retries for any method, no other status retries, and `delay_seconds` is unchanged.\n\nA request is **idempotent** when repeating it has the same intended effect on server state as sending it once.\n\nVerified statuses 100–599 across eight methods and case variants, the default method, delay values and invalid attempts, JSON fields and types, and `git diff --check`."
}
