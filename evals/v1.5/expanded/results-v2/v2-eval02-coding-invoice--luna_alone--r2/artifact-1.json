{
  "artifacts": {
    "invoice.py": "from dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Line:\n    sku: str\n    unit_cents: int\n    quantity: int\n\ndef quote(lines, region, member=False):\n    if region not in {'domestic', 'international'}:\n        raise ValueError('region')\n    subtotal = 0\n    for line in lines:\n        if line.unit_cents < 0 or line.quantity < 1:\n            raise ValueError('line')\n        subtotal += line.unit_cents * line.quantity\n    discount = subtotal // 10 if member and subtotal >= 5000 else 0\n    merchandise = subtotal - discount\n    if subtotal == 0:\n        shipping = 0\n    elif region == 'international':\n        shipping = 2000\n    else:\n        shipping = 0 if merchandise >= 5000 else 700\n    return {\n        'subtotal_cents': subtotal,\n        'discount_cents': discount,\n        'shipping_cents': shipping,\n        'total_cents': merchandise + shipping,\n    }\n"
  },
  "public_final": "Updated [invoice.py](<disposable>/workspace/invoice.py) to support `member=False`, member discounts, and the requested shipping rules while keeping `Line` and existing `quote(lines, region)` calls working.\n\nVerified empty orders, domestic shipping thresholds before and after discount, international shipping, and `ValueError` for negative unit cents, quantity below 1, and unknown regions. All checks passed."
}
