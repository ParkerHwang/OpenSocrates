{
  "packet_id": "0c33f05577d34526",
  "task_id": "v2-eval01-coding-transition",
  "locale": "en",
  "initial_request": "Add an optional savings_cents=0 argument to offer.quote. Savings must be a nonnegative integer and cannot exceed the computed subtotal; reject invalid savings with ValueError. Keep the catalog lookup as the authority for plan aliases, prices, and seat limits. Preserve quote's integer return. Public decision for later work: follow current catalog.lookup rather than copying catalog values into offer.py. Verify the changed behavior and report the decision in public terms.",
  "followup_request": "Fresh session: catalog.py changed after the earlier accepted decision. Keep offer.quote(label, seats, savings_cents=0) returning an integer. Add offer.breakdown(label, seats, savings_cents=0), returning a dictionary with plan_code (string) and subtotal_cents, savings_cents, total_cents (integers). Savings must be a nonnegative integer no greater than the subtotal; invalid savings and seats outside the current catalog plan limit raise ValueError. Use current catalog.lookup for alias, price, and plan code, including squad. notes.md is historical. Update offer.py and report the result; do not treat an old alias or a memory receipt as current code evidence.",
  "source_before_task": {
    "catalog.py": "PLANS = {'starter': {'code': 'starter', 'seat_cents': 1000, 'max_seats': 5}, 'team': {'code': 'team', 'seat_cents': 800, 'max_seats': 12}, 'studio': {'code': 'studio', 'seat_cents': 650, 'max_seats': 8}}\nALIASES = {'squad': 'studio'}\n\ndef lookup(label):\n    return PLANS[ALIASES.get(label, label)]\n",
    "offer.py": "from catalog import lookup\n\ndef quote(label, seats, savings_cents=0):\n    plan = lookup(label)\n    if seats < 1 or seats > plan['max_seats']:\n        raise ValueError('seats')\n    subtotal = plan['seat_cents'] * seats\n    if type(savings_cents) is not int or savings_cents < 0 or savings_cents > subtotal:\n        raise ValueError('savings_cents')\n    return subtotal - savings_cents\n",
    "notes.md": "Old marketing copy: squad means team. This sentence is historical; catalog.py is the current source for aliases and price.\n"
  },
  "declared_source_transition": {
    "catalog.py": "PLANS = {'starter': {'code': 'starter', 'seat_cents': 1000, 'max_seats': 5}, 'team': {'code': 'team', 'seat_cents': 800, 'max_seats': 12}, 'studio': {'code': 'studio', 'seat_cents': 650, 'max_seats': 8}}\nALIASES = {'squad': 'studio'}\n\ndef lookup(label):\n    return PLANS[ALIASES.get(label, label)]\n"
  },
  "scoped_public_intent": "Use current catalog.lookup as the authority for aliases, seat prices, and limits; apply validated savings after lookup, never hardcode yesterday's squad mapping.",
  "stages": [
    {
      "artifacts": {
        "offer.py": "from catalog import lookup\n\ndef quote(label, seats, savings_cents=0):\n    return breakdown(label, seats, savings_cents)['total_cents']\n\ndef breakdown(label, seats, savings_cents=0):\n    plan = lookup(label)\n    if type(seats) is not int or seats < 1 or seats > plan['max_seats']:\n        raise ValueError('seats')\n    subtotal = plan['seat_cents'] * seats\n    if type(savings_cents) is not int or savings_cents < 0 or savings_cents > subtotal:\n        raise ValueError('savings_cents')\n    return {\n        'plan_code': plan['code'],\n        'subtotal_cents': subtotal,\n        'savings_cents': savings_cents,\n        'total_cents': subtotal - savings_cents,\n    }\n"
      },
      "public_final": "Updated [offer.py](<disposable>/workspace/offer.py). `breakdown()` returns the requested fields using the current `catalog.lookup`; `quote()` still returns an integer. Invalid savings and seats raise `ValueError`.\n\nChecked all current plans, including `squad` resolving to `studio`, calculation and validation boundaries, and `git diff --check`. All passed. The pre-existing `catalog.py` change was left intact.\n\n[treatment label removed] native activation is unconfirmed; this was a mechanical edit."
    }
  ],
  "designated_artifact_diffs": {
    "offer.py": "--- before/offer.py\n+++ after/offer.py\n@@ -1,10 +1,18 @@\n from catalog import lookup\n \n def quote(label, seats, savings_cents=0):\n+    return breakdown(label, seats, savings_cents)['total_cents']\n+\n+def breakdown(label, seats, savings_cents=0):\n     plan = lookup(label)\n-    if seats < 1 or seats > plan['max_seats']:\n+    if type(seats) is not int or seats < 1 or seats > plan['max_seats']:\n         raise ValueError('seats')\n     subtotal = plan['seat_cents'] * seats\n     if type(savings_cents) is not int or savings_cents < 0 or savings_cents > subtotal:\n         raise ValueError('savings_cents')\n-    return subtotal - savings_cents\n+    return {\n+        'plan_code': plan['code'],\n+        'subtotal_cents': subtotal,\n+        'savings_cents': savings_cents,\n+        'total_cents': subtotal - savings_cents,\n+    }\n"
  },
  "human_status": "unavailable",
  "human_scores": null,
  "non_blindable_cues": [
    "Memory/note references and characteristic wording may reveal condition; complete blinding is not guaranteed."
  ],
  "coverage_limit": "Packets contain declared task sources and designated output artifacts, not a complete post-run filesystem snapshot or raw tool transcript."
}
