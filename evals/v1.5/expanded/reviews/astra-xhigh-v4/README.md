# Final citation-format continuation

This separately frozen continuation retains 56 completed evidence assessments
and all 60 original first-pass judgments. It reviews four packets: two from A16P1 whose two v3 responses failed citation validation,
and two from the A17P1 call that reached its 900-second limit. One response used
`deterministic:/`; its permitted fresh retry used bare `deterministic:`. Both
responses and their usage remain retained and rejected under the original rule.

The four new assignments contain one packet each. The prompt explicitly requires a
named, existing evidence field, using a coverage/limitation field when appropriate
for an absence claim. This is format guidance only. The reviewer receives no
rejected score, rejected response, treatment map, implementation conversation,
other assignment or semantic correction. Every supplied first-pass packet object
is still identical to its original locked judgment.

The verified `opensocrates_bilingual_reviewer` definition, `gpt-6-astra`, `xhigh`,
read-only sandbox, client, schema and fixed rubric remain unchanged. Two calls
may run concurrently, bounded to 900 seconds and 256 KiB each; the same single
format/citation retry and stop-dispatch policy applies. No candidate artifact or
historical outcome is repaired or rerun. No validator or rubric rule is relaxed.

The combined 60 evidence assessments must validate and lock before any analytic
unblinding. All v1–v4 attempts remain in the ledger. Human scores remain unavailable;
these are provisional Astra assessments, not human review or release approval.
