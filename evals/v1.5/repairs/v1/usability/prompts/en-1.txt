Plan a synthetic venue event for 48 attendees with accepted step-free access. Do not book. Read venues.json and choose Cedar if feasible. Write plan.json matching the exact artifact contract below. After the first milestone (source inspection and venue choice), actually create a nonempty task checkpoint. Validate the artifact against the sources and contract, inspect the saved checkpoint's actual current version, then update it with a second nonempty completed action. Inspect the resulting saved state. These are your own reported actions: use agent_reported support. Do not claim native observation, repair the adapter, guess repeated payloads, or seed a different task. Preserve the accepted project decision. Use the installed bilingual checkpoint guide and actual request schema.
