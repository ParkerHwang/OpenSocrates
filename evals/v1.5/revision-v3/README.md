# One targeted Korean recovery retest

The original revision-v2 outcome is frozen: both plans, official-document answers
and exact deletions passed; the Korean call failed v1.1 preparation/recall and did
not update its checkpoint. Both locales also guessed invalid assistance fields or
enums. This boundary tests one diagnosed recovery change, not a favorable repeat
of an unchanged treatment.

The new package includes complete v1.1 memory request examples and assistance
guide8. Rejected native requests identify known fields and allowed values without
echoing the rejected content; legacy `prepare` points to its required request
version and example. Contracts are not relaxed to accept guessed synonyms.

Use the identical Korean task, source transition, expected artifact and code runner,
with a fresh synthetic task/profile and exactly one Luna/medium call (300 seconds,
no retry/escalation). No prior outcome or repair answer is delivered. Before the
call, commit the new manifest and exact client/package/guide/helper hashes.
The evaluation adapter now permits the existing read-only `export` operation:
its earlier denial was a harness restriction, not an OpenSocrates export failure.
This change makes the conditions distinct and is not pooled into the old score.

All other privacy, permissions, usage/null accounting, source-read and stop rules
are inherited explicitly from the original [boundary](../revision-v2/README.md).
The native adapter and official-page reader remain evaluation tools, not product
capabilities or stronger-model assistance. Human scores stay unavailable.

From the repository root, use `python3 evals/v1.5/revision-v3/runner.py preflight`
or `execute` with `--manifest-sha256 <new-frozen-digest>`. Exclusive creation prevents
overwriting an old attempt. The initial two calls and this retest are reported
separately. Stop after this diagnosed retest; do not commission another matrix.
