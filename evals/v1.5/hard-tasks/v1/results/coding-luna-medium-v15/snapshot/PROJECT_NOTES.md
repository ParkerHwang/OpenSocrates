Implemented the tenant-scoped AuditLedger in `internal/platform/driver.go`; startup performs transactional v0/v1-to-v2 setup/import, and writes persist operation responses with their effects. The API supports current and entry-derived historical reads, holds, batches, and reversals. `cmd/server/main.go` now serves HTTP and gracefully shuts down. `README.md` documents run and storage behavior.

Checks completed: `go test ./...`, `go build -o server ./cmd/server`, local HTTP lifecycle/replay/conflict/snapshot/capture/reversal checks, supplied legacy producer migration with source-row preservation, and concurrent identical writes through two server processes. Both servers were stopped. The performance sweep was intentionally not run. The local launcher emitted a macOS `xcrun_db` cache warning before Python executions; Python checks themselves completed successfully.
