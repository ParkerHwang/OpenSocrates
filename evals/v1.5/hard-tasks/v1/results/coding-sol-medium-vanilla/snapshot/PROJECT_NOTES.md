Implemented AuditLedger in `internal/ledger/ledger.go`, HTTP startup and graceful shutdown in `cmd/server/main.go`, and focused integration tests in `internal/ledger/ledger_test.go`. SQLite v2 tables are accounts, holds, transfers, entries, and retries; legacy tables remain intact. Every write and migration uses BEGIN IMMEDIATE. One SQLite connection per process keeps its busy timeout configured. All visible state and successful retry responses are durable in SQLite.

Verified with `go build -o server ./cmd/server`, `go test ./...`, and `go vet ./...`. Manual localhost checks covered account creation, transfer, hold creation/release/capture, reversal including insufficient funds and terminal states, failed-key reuse, historical summaries, entry pagination, tenant isolation, genuine `legacy/produce.py` migration with note preservation, and identical concurrent batch requests against two processes. All three test servers were stopped and their ports checked closed. No performance sweep was run.
