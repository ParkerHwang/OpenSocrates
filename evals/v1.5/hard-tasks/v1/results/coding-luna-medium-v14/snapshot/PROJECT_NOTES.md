Implement the stated synthetic AuditLedger contracts with tenant isolation, atomic durable changes, retry identity and preserved legacy evidence. Do not deploy, call other models, or change global settings. Existing source facts must be inspected rather than inferred from notes.
Implementation status (2026-09-27): API and SQLite schema now live in `internal/platform/api.go`; startup/migration is called by `cmd/server/main.go`. V1 migration is performed after v2 tables are created inside a transaction. Final `go build -o <TEMP_PATH> ./cmd/server` passed. Local HTTP checks covered v1 import, retry replay, hold capture, reversal, historical summary, tenant isolation, validation, terminal conflicts, batch rollback, and matching concurrent requests through two processes. The v1 database retained all three legacy notes and movements and reached `user_version=2`. The independent acceptance/performance evaluation was not run here; the performance sweep remains intentionally unrun.
