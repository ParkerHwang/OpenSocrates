AuditLedger implementation is in `internal/ledger/ledger.go`; `cmd/server/main.go` starts and gracefully shuts down the HTTP server. The existing platform driver and legacy fixture producer are preserved. Build with `GOTOOLCHAIN=local GOPROXY=off go build -o server ./cmd/server`.

Verified locally: build and `go test ./...`, including the HTTP regression test in `internal/ledger/ledger_test.go`; live HTTP exercise of account creation, transfer, staged batch versions and rollback, hold capture and release, reversal, idempotency replay/conflict, entries pagination, historical summary, tenant-local legacy migration on the supplied and a separate generated fixture, restart replay, concurrent writes from two processes, simultaneous legacy startup, same-key race, and failed-key reuse. The live checks used temporary SQLite files and stopped their servers. No performance sweep was run; independent measurement is pending after this session.

Design constraints: writes use one `BEGIN IMMEDIATE` transaction including the stored successful response. Reads choose and use their snapshot inside one read transaction. Legacy v1 import preserves old tables and notes and advances `user_version` only on commit. Business state is not kept in process memory. The API contract remains `TASK.md`; this note is a bounded work record, not a substitute for current source review.
