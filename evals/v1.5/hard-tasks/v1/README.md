# Hard coding and office comparison

User-authorized follow-up: Sol medium, Luna medium and Luna max on difficult coding
and general office work. Retain vanilla Codex, released OpenSocrates1.4 and the
current1.5 RC as separate conditions: **18 planned calls**, one fresh single-session
episode per tuple/version/domain. This is a bounded practical comparison, not a
held-out superiority study or a release prerequisite.

Two Sol/high preparation authors independently wrote the coding and office packets
in isolated worktrees without viewing candidate outcomes. The office author made
one follow-up repair after integrator review. Three delegated preparation turns
have unavailable usage, separate from outcome calls. No preparation agent supplies
answers or repairs inside an outcome. Primary keeps the selected model and effort.

## Task and evidence boundaries

- Coding: AuditLedger, a durable tenant-scoped Go/SQLite resource ledger with11
  routes, atomic batches, holds/capture/release, reversals, optimistic versions,
  idempotency, historical summaries/pagination and genuine old-format migration.
  Give the model only TASK.md and the generic starter. The hidden28-group checker
  uses exact requests/responses, independent transactions, restarts and two processes.
- Office: a Korean regional-training operations decision using12 source files and
  263 tabular rows. Reconcile invoices/credits/payments and revisions, correct HR
  and predecessor assumptions, optimize an authorized allocation, and deliver CSV,
  JSON, a six-sheet XLSX and a Korean memo. Give only TASK.md and inputs, never the
  oracle/example assignment. The hidden checker has27 grouped checks; prose uses
  the fixed unblinded primary-review rubric separately.
- Both receive identical facts, permissions, tools and a competent maintained-note
  starting control. The candidate additionally has optional explicitly enrolled
  synthetic native memory containing the same accepted intent, without extra facts.
  No memory calls are required solely to satisfy a counter. A single session does
  not establish a fresh-session memory effect.

The common CLI client, Go1.26.3/SQLite1.59.0, bundled Python3.12.14/openpyxl3.1.5,
packages, guides, tasks, checkers, failure handling and usage fields are frozen in
manifest.json before any outcome call. Clone a freshly primed generic-driver cache
per coding cell; no prior implementation cache or candidate answer is supplied.
The released archive must be the real published1.4 package, not a development ZIP
whose metadata/name still says1.4. No active installation or global settings change.

## Execution and stop rules

Each call has1200 seconds; no model substitution, stronger-model subagent, automatic
outcome retry or post-outcome integrator code repair. Count every CLI start and tool
attempt. The first planned call for each tuple checks actual client/account access;
catalog support alone is not proof. An unchanged unsupported tuple is skipped, not
repeated or downgraded. Account-wide quota/authentication failures stop other calls;
no reset credit or new paid service is used. A task timeout remains a failed/incomplete
attempt with any retained artifact assessed separately.

Use at most two concurrent builders in balanced six rounds. Their invocation times
may overlap another build/model and are descriptive, not isolated speed estimates.
Only after all generation and builds finish, run serial performance cells on the
same host. Each coding binary gets a public-API10002-entry seed, fresh SQLite clones,
read/posting workloads at concurrency1/16, three repetitions,1-second warmup and
5-second measurement. Count all starts/completions/failures/drains. Throughput
credits successful requests that start and finish in the window; latency follows
the start cohort including drain. Warmup carry-in is separate. CPU/RSS describe the
server and sampling scope, not whole-machine cost or a guaranteed peak.

Performance is fully eligible only when all28 independent checks, own test command,
protected inputs and cell conservation pass. Otherwise valid numbers are diagnostic;
unavailable measurements are null. Repetitions are load samples of one artifact,
not independent model replications. Do not rank a broken server as a qualified win.

Source correctness, delivery/operation evidence, model quality, application and
cost are separate. A valid pack does not prove use. The evaluator preserves all
public messages, failures, native-adapter attempts and command-output projections.
Unknown command outcomes remain unknown. No raw model event stream/private reasoning
is retained. Raw synthetic source/data/timing evidence stays in this declared
evaluation boundary; product memory never stores raw prompts/transcripts/source
copies. Remove disposable auth copies promptly after each cell.

Office completion requires all mandatory artifacts and27 checks; unsupported
uncached spreadsheet formulas are explicitly unassessable. Judge false approval,
missing evidence, usefulness and Korean clarity from complete public messages and
the memo. Primary prose scores are provisional/unblinded, not human ratings. Coding
review documents concrete ownership/reuse/transaction/test findings with source
citations; line counts and passing self-tests alone are not quality scores.

## Preparation validation

Positive office static/formula/alternative-plan controls and deliberate bad
accounting/approval/access/calendar/evidence mutations passed. Integrator corrected
Excel ROUND tie behavior, literal formula lexing and explicit unsupported-criteria
handling before freeze. Independent arithmetic reproduces gross881200, credits6600,
payments87000 and reserve787600. These oracle values are hidden from candidates.
Coding validates comparison primitives, real legacy producer, bad HTTP responses,
isolated startup failure and meter windows/nulls. Integrator excluded warmup
carry-in from start-cohort throughput before freeze. No full reference backend is
provided, so positive API coverage depends on actual generated implementations.

Reproduction uses runner.py modes `preflight`, `execute`, `qualify`, `measure`, with
the frozen manifest SHA and explicit local storage/package/toolchain paths. Result
files are exclusive; a diagnosed harness correction must use a new named evidence
boundary. Do not edit old sources, outcomes, scores or rubrics to make failures pass.
The original comparison and this expanded requested comparison remain distinct.

`plan_objective_measure`: compare difficult coding and office outcomes under the
three requested tuples and competent version controls, with observed work/usage.
`do_scope`:18 initial model calls and at most108 serial backend load cells.
`check_rule`: prospective hashes, independent oracles, actual artifacts/public
messages, protected-input checks, all attempts and explicit missingness.
`act_standardize_decision`: report wins/ties/regressions and limits; no profile
promotion, publication or further study is automatic.
